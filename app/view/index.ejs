<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>BookTrack | Manajemen Buku</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
    }
  </style>
</head>
<body class="bg-light">

<%- include('partials/navbar') %>

<div class="container mt-4">

  <!-- FORM TAMBAH BUKU -->
  <div class="card shadow mb-4">
    <div class="card-body">
      <h5 class="mb-3">➕ Tambah Buku</h5>

      <form action="/add" method="POST">
        <div class="row g-2">
          <div class="col-md-3">
            <input type="text" name="judul" class="form-control" placeholder="Judul Buku" required>
          </div>
          <div class="col-md-3">
            <input type="text" name="penulis" class="form-control" placeholder="Penulis" required>
          </div>
          <div class="col-md-2">
            <input type="number" name="tahun" class="form-control" placeholder="Tahun" required>
          </div>
          <div class="col-md-2">
            <select name="status" class="form-select">
              <option value="tersedia">tersedia</option>
              <option value="dipinjam">dipinjam</option>
            </select>
          </div>
          <div class="col-md-2">
            <button class="btn btn-primary w-100">Tambah</button>
          </div>
        </div>
      </form>
    </div>
  </div>

  <!-- TABEL DATA BUKU -->
  <div class="card shadow">
    <div class="card-body">
      <h5 class="mb-3">📖 Daftar Buku</h5>

      <table class="table table-striped table-hover align-middle">
        <thead class="table-dark">
          <tr>
            <th>Judul</th>
            <th>Penulis</th>
            <th>Tahun</th>
            <th>Status</th>
            <th class="text-center">Aksi</th>
          </tr>
        </thead>
        <tbody>
          <% if (books.length === 0) { %>
          <tr>
            <td colspan="5" class="text-center text-muted">Belum ada data buku</td>
          </tr>
          <% } else { %>
            <% books.forEach(buku => { %>
            <tr>
              <td><%= buku.judul %></td>
              <td><%= buku.penulis %></td>
              <td><%= buku.tahun %></td>
              <td>
                <% if (buku.status === 'Tersedia') { %>
                  <span class="badge bg-success">Tersedia</span>
                <% } else { %>
                  <span class="badge bg-danger">Dipinjam</span>
                <% } %>
              </td>
              <td class="text-center">
                <% if (buku.status === 'Tersedia') { %>
                  <a href="/pinjam/<%= buku.id %>" class="btn btn-info btn-sm text-white">Pinjam</a>
                <% } %>
                <a href="/edit/<%= buku.id %>" class="btn btn-warning btn-sm">Edit</a>
                <form action="/delete/<%= buku.id %>" method="POST" style="display:inline;">
                  <button type="submit" class="btn btn-danger btn-sm"
                          onclick="return confirm('Yakin hapus buku ini?')">
                    Hapus
                  </button>
                </form>
              </td>
            </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>

    </div>
  </div>

</div>

<%- include('partials/footer') %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>