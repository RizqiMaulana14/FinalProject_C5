<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pinjam Buku | BookTrack</title>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

<%- include('partials/navbar') %>

<div class="container mx-auto px-4 py-8 flex-grow">
  <div class="max-w-3xl mx-auto">
    
    <!-- Header Section -->
    <div class="mb-6">
      <h1 class="text-3xl font-bold text-gray-800 flex items-center gap-2">
        📖 Pinjam Buku
      </h1>
      <p class="text-gray-600 mt-2">Isi formulir peminjaman buku di bawah ini</p>
    </div>

    <!-- Progress Steps -->
    <div class="mb-8">
      <div class="flex items-center justify-center">
        <div class="flex items-center">
          <div class="flex items-center text-blue-600">
            <div class="rounded-full h-10 w-10 bg-blue-600 text-white flex items-center justify-center font-semibold shadow-lg">
              1
            </div>
            <span class="ml-3 text-sm font-semibold">Pilih Buku</span>
          </div>
          <div class="flex-auto border-t-2 border-blue-600 mx-4"></div>
          <div class="flex items-center text-blue-600">
            <div class="rounded-full h-10 w-10 bg-blue-600 text-white flex items-center justify-center font-semibold shadow-lg">
              2
            </div>
            <span class="ml-3 text-sm font-semibold">Isi Data</span>
          </div>
          <div class="flex-auto border-t-2 border-gray-300 mx-4"></div>
          <div class="flex items-center text-gray-400">
            <div class="rounded-full h-10 w-10 bg-gray-300 text-white flex items-center justify-center font-semibold">
              3
            </div>
            <span class="ml-3 text-sm font-semibold">Selesai</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Form Card -->
    <div class="bg-white rounded-lg shadow-lg overflow-hidden">
      
      <!-- Book Info Section -->
      <div class="bg-gradient-to-r from-blue-500 to-blue-600 p-6 text-white">
        <h5 class="text-lg font-semibold mb-4 flex items-center gap-2">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
          </svg>
          Informasi Buku
        </h5>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <div>
            <p class="text-blue-100 text-sm mb-1">Judul Buku</p>
            <p class="font-semibold text-lg"><%= book.judul %></p>
          </div>
          <div>
            <p class="text-blue-100 text-sm mb-1">Penulis</p>
            <p class="font-semibold text-lg"><%= book.penulis %></p>
          </div>
          <div>
            <p class="text-blue-100 text-sm mb-1">Tahun Terbit</p>
            <p class="font-semibold text-lg"><%= book.tahun %></p>
          </div>
        </div>
      </div>

      <!-- Form Section -->
      <div class="p-8">
        <form action="/pinjam/<%= book.id %>" method="POST" class="space-y-6">
          
          <div class="mb-6">
            <h5 class="text-lg font-semibold text-gray-800 mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
              Data Peminjam
            </h5>
            
            <!-- Nama Peminjam -->
            <div class="mb-5">
              <label class="block text-gray-700 text-sm font-semibold mb-2">
                Nama Lengkap Peminjam <span class="text-red-500">*</span>
              </label>
              <input 
                type="text" 
                name="nama_peminjam" 
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200 outline-none" 
                placeholder="Masukkan nama lengkap peminjam"
                required>
              <p class="text-xs text-gray-500 mt-1">Pastikan nama yang dimasukkan sesuai dengan identitas</p>
            </div>

            <!-- Tanggal Pinjam -->
            <div>
              <label class="block text-gray-700 text-sm font-semibold mb-2">
                Tanggal Peminjaman <span class="text-red-500">*</span>
              </label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                  <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                  </svg>
                </div>
                <input 
                  type="date" 
                  name="tanggal_pinjam" 
                  class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200 outline-none" 
                  value="<%= new Date().toISOString().split('T')[0] %>" 
                  required>
              </div>
              <p class="text-xs text-gray-500 mt-1">Tanggal otomatis terisi dengan hari ini</p>
            </div>
          </div>

          <!-- Info Box -->
          <div class="bg-blue-50 border-l-4 border-blue-500 p-4 rounded-r-lg">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div class="ml-3">
                <p class="text-sm text-blue-800 font-medium">Ketentuan Peminjaman</p>
                <p class="text-xs text-blue-700 mt-1">
                  • Batas waktu peminjaman maksimal 14 hari<br>
                  • Harap kembalikan buku dalam kondisi baik<br>
                  • Keterlambatan pengembalian akan dikenakan denda
                </p>
              </div>
            </div>
          </div>

          <!-- Divider -->
          <hr class="border-gray-200">

          <!-- Action Buttons -->
          <div class="flex flex-col sm:flex-row gap-3">
            <button 
              type="submit"
              class="flex-1 bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition duration-200 font-semibold shadow-md hover:shadow-lg transform hover:-translate-y-0.5 flex items-center justify-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
              Konfirmasi Peminjaman
            </button>
            <a 
              href="/" 
              class="flex-1 bg-gray-500 text-white px-6 py-3 rounded-lg hover:bg-gray-600 transition duration-200 font-semibold text-center shadow-md hover:shadow-lg transform hover:-translate-y-0.5 flex items-center justify-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
              </svg>
              Batal
            </a>
          </div>

          <!-- Helper Text -->
          <p class="text-sm text-gray-500 text-center mt-4">
            <span class="text-red-500">*</span> Wajib diisi
          </p>
        </form>

      </div>
    </div>

  </div>
</div>

<%- include('partials/footer') %>

</body>
</html>